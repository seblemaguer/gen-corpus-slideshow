# Corpus Slideshow generation

Utilities to convert a batch of text files to a beamer slideshow

## How to use

```sh
    python gen_slideshow.py --template "assets/default.tex" test output.pdf
```

The results will be the file `output.pdf`
